# ===================================================================
# CONFIGURA��O PADR�O (default) - Para desenvolvimento local com H2
# ===================================================================
# Nome da aplica��o
spring.application.name=equipamento-service

# Configura��o do Servidor
server.port=8081

# Configura��o do Banco de Dados H2 em Mem�ria
spring.datasource.url=jdbc:h2:mem:scb-equipamento;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Configura��o do JPA/Hibernate para desenvolvimento
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Configura��o do Console H2 (habilitado apenas em desenvolvimento)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console